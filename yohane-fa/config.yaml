trainer:
  max_epochs: 100
  callbacks:
    - class_path: EarlyStopping
      init_args:
        monitor: val_frame_accuracy
        mode: max

data:
  dataset_path: NextFire/karaoke-alignments
  batch_size: 8
  num_workers: 4
